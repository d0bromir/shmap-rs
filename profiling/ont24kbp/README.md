# Real ONT at ~24 kb — raw artifacts

Numbers live in [`RESULTS.md`](../../RESULTS.md) §2. This directory holds the `-x` reports,
`/usr/bin/time -v` records, the driver scripts and `runs.tsv` (the 29 ENA runs streamed).

Reads: 92 220 real HG002 Oxford Nanopore reads, mean 23 760 bp, 2.19 Gbp, 0.7029x of
T2T-CHM13. Streamed from ENA and filtered to a 20-28 kb band on the fly.

